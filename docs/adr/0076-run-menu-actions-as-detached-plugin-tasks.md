# Run menu actions as detached plug-in tasks

The runtime will project a native inventory gesture into an immutable framework value before launching its menu action as ordinary supervised plug-in coroutine work. Actions receive stable player references rather than live Paper objects and enter the kernel's explicit entity, region, or global ownership blocks for direct server access. Native reconciliation, cursor changes, remounts, feedback, and recovery remain framework operations that perform their own entity-safe transitions.
