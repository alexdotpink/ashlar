# Expose semantic command argument types

The command module will cover the full supported Paper and Minecraft argument catalogue with semantic Kotlin types. Immutable values, keys, collections, and copied data may pass directly; server-owned values use safe framework references, while Paper resolver and provider classes remain inside the adapter. Greedy text consumes the decoded positional remainder after option extraction, and suggestion requests cancel obsolete work without imposing a framework cache.
