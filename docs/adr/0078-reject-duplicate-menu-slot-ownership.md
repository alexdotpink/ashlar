# Reject duplicate menu slot ownership

Each physical host slot will have exactly one declarative owner in a successful render. Duplicate claims fail with the coordinate and both component paths rather than depending on declaration order or numeric priority. Intentional augmentation uses typed slot modifiers passed through component interfaces, keeping composition explicit and preserving one atomic handler and storage binding per slot.
