# Layout chests with coordinates and regions

Chest menus will expose exact slot indexes, row-column coordinates, and explicitly bounded regions that flow keyed content. The module will not impose a resizable constraint or flexbox model on Minecraft's fixed nine-column inventory. Regions centralize repeated placement and pagination math without hiding which slots a render occupies.
