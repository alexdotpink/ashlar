# Model native host semantics declaratively

Each concrete menu host will expose its actual typed properties, slot roles, and interaction protocol and translate native input into the shared immutable gesture and atomic transaction model. Host adapters will not allow vanilla mutation followed by observation or repair, and special hosts will not be reduced to generic chest-shaped storage. Time-based processing such as furnace progress occurs only through explicitly declared state and effects rather than hidden simulation.
